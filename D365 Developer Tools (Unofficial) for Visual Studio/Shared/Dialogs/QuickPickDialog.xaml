<vsp:DialogWindow x:Class="D365_Developer_Tools__Unofficial__for_Visual_Studio.Shared.Dialogs.QuickPickDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:D365_Developer_Tools__Unofficial__for_Visual_Studio.Shared.Dialogs"
        xmlns:vsp="clr-namespace:Microsoft.VisualStudio.PlatformUI;assembly=Microsoft.VisualStudio.Shell.15.0"
        x:ClassModifier="internal"
        Height="440" Width="520"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False"
        Background="{DynamicResource {x:Static vsp:EnvironmentColors.ToolWindowBackgroundBrushKey}}"
        Foreground="{DynamicResource {x:Static vsp:EnvironmentColors.ToolWindowTextBrushKey}}">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="../Styles/ThemedControls.xaml" />
            </ResourceDictionary.MergedDictionaries>

        <local:EmptyToCollapsedConverter x:Key="EmptyToCollapsedConverter" />

        <DataTemplate x:Key="SingleTemplate">
            <StackPanel Margin="6,4" MouseLeftButtonUp="OnRowClicked">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="{Binding Label}" FontWeight="SemiBold" />
                    <TextBlock Text="{Binding Description}" Margin="8,0,0,0" Opacity="0.7" />
                </StackPanel>
                <TextBlock Text="{Binding Detail}" Opacity="0.6" FontSize="11"
                           Visibility="{Binding Detail, Converter={StaticResource EmptyToCollapsedConverter}}" />
            </StackPanel>
        </DataTemplate>

        <DataTemplate x:Key="MultiTemplate">
            <StackPanel Orientation="Horizontal" Margin="2">
                <CheckBox IsChecked="{Binding IsChecked, Mode=TwoWay}" VerticalAlignment="Top" Margin="2,4,6,0" />
                <StackPanel>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="{Binding Label}" FontWeight="SemiBold" />
                        <TextBlock Text="{Binding Description}" Margin="8,0,0,0" Opacity="0.7" />
                    </StackPanel>
                    <TextBlock Text="{Binding Detail}" Opacity="0.6" FontSize="11"
                               Visibility="{Binding Detail, Converter={StaticResource EmptyToCollapsedConverter}}" />
                </StackPanel>
            </StackPanel>
        </DataTemplate>
        </ResourceDictionary>
    </Window.Resources>

    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBox x:Name="SearchBox" Grid.Row="0" Padding="4" Margin="0,0,0,8" TextChanged="OnSearchTextChanged" />

        <ListBox x:Name="ItemsList" Grid.Row="1" MouseDoubleClick="OnRowDoubleClicked"
                  KeyDown="OnListKeyDown" />

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="OK" Width="80" Click="OnOk" Margin="0,0,8,0" />
            <Button Content="Cancel" Width="80" IsCancel="True" Click="OnCancel" />
        </StackPanel>
    </Grid>
</vsp:DialogWindow>
